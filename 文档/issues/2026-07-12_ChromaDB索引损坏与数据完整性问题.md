# 2026-07-12 ChromaDB 索引损坏与数据完整性问题

> 会话时间：2026-07-11 ~ 2026-07-12
> 状态：**待修复**

---

## 一、问题总览

| 编号 | 问题 | 严重程度 | 状态 |
|------|------|----------|------|
| P1 | HNSW 索引损坏（pickle 文件截断） | 高 | 部分修复 |
| P2 | 混合检索 query() 失败 | 高 | 未修复 |
| P3 | LeCaRD 入库未完成（1019 条待处理） | 中 | 未修复 |
| P4 | get_all_documents() SQL 变量溢出 | 中 | 已修复 |

---

## 二、问题详情

### P1：HNSW 索引损坏（pickle 文件截断）

**现象**：
```
chromadb.errors.InternalError: Error executing plan: Error sending backfill request to compactor:
Error constructing hnsw segment reader: Error creating hnsw segment reader:
Error deserializing pickle file: eval error at offset 29609312: trailing bytes found
```

**原因**：SCL 数据集入库（22552 个文件）过程中，HNSW 索引的 `index_metadata.pickle` 文件被截断。文件大小 10562001 字节，但 pickle 数据不完整。

**临时修复**：删除了损坏的 pickle 文件
```bash
rm -f data/chroma/3cc4021b-706b-457e-8296-799b75c12072/index_metadata.pickle
```

**当前状态**：`col.get()` 可以正常工作，但 `col.query()` 仍有问题（见 P2）。

**损坏文件路径**：
```
data/chroma/3cc4021b-706b-457e-8296-799b75c12072/index_metadata.pickle  (已删除)
data/chroma/3cc4021b-706b-457e-8296-799b75c12072/data_level0.bin        (1.4GB)
data/chroma/3cc4021b-706b-457e-8296-799b75c12072/header.bin
data/chroma/3cc4021b-706b-457e-8296-799b75c12072/length.bin
data/chroma/3cc4021b-706b-457e-8296-799b75c12072/link_lists.bin
```

---

### P2：混合检索 query() 失败

**现象**：
```
chromadb.errors.InternalError: Error executing plan: Internal error: Error finding id
```

**复现步骤**：
```bash
HF_ENDPOINT=https://hf-mirror.com PYTHONPATH=. uv run python scripts/search_test.py --hybrid "民法典第1043条"
```

**调用链**：
1. `search_test.py` → `retriever.search()`
2. `retriever.py:125` → `vector_store.get_all_documents()` （构建 BM25 索引）
3. `retriever.py:175` → `vector_store.search()` → `collection.query()` ← **失败**

**已验证正常的操作**：
- `col.get(limit=3)` ✅
- `col.get(limit=1000)` ✅
- `col.get(limit=5000, offset=0)` ✅（分批获取）
- `col.query(query_embeddings=[...], n_results=3)` ✅（通过 Python 直接调用）

**异常的操作**：
- `col.query()` 在 `retriever.py` 的混合检索流程中调用时失败 ❌

**可能原因**：
1. HNSW 索引不完整（pickle 被删除后索引结构异常）
2. BM25 索引构建过程中 ChromaDB 状态异常
3. 并发访问问题

**当前影响**：
- 单独的向量搜索可以工作
- 混合检索（BM25 + 向量）无法工作
- FastAPI 服务和 Streamlit 界面的问答功能受影响

---

### P3：LeCaRD 入库未完成

**现象**：LeCaRD 数据集（3795 个裁判文书）入库过程中，进程被手动终止。

**当前状态**：
- SQLite 中有 1019 条待处理记录：`SELECT COUNT(*) FROM embeddings_queue` = 1019
- 总嵌入数：656687 条（含 SCL 和部分 LeCaRD）
- ChromaDB 文件大小：2.6GB

**待处理数据**：
```
data/datasets/lecard/texts/  →  3795 个 .txt 文件
部分已入库，剩余约 1019 条在队列中等待处理
```

---

### P4：get_all_documents() SQL 变量溢出（已修复）

**现象**：
```
chromadb.errors.InternalError: Error executing plan: Internal error: error returned from database:
(code: 1) too many SQL variables
```

**原因**：`collection.get()` 一次获取 619K+ 文档时，SQLite 参数化查询超出变量限制。

**修复方案**：修改 `app/rag/vector_store.py` 的 `get_all_documents()` 函数，改为分批获取（每批 5000 条）。

**修复代码**：
```python
def get_all_documents() -> dict:
    """Return all documents from the collection (ids, documents, metadatas).
    Uses batched fetching to avoid SQL variable overflow with large collections.
    """
    collection = _get_collection()
    count = collection.count()
    if count == 0:
        return {"ids": [], "documents": [], "metadatas": []}

    all_ids, all_docs, all_metas = [], [], []
    batch_size = 5000
    for offset in range(0, count, batch_size):
        batch = collection.get(limit=batch_size, offset=offset)
        all_ids.extend(batch["ids"])
        all_docs.extend(batch["documents"])
        all_metas.extend(batch["metadatas"])
    return {"ids": all_ids, "documents": all_docs, "metadatas": all_metas}
```

---

## 三、数据状态

### ChromaDB 当前数据

| 指标 | 值 |
|------|-----|
| 集合名称 | legal_knowledge |
| 向量维度 | 512（bge-small-zh） |
| 嵌入总数 | 656687 |
| 待处理队列 | 1019 |
| 数据库大小 | 2.6GB |
| HNSW 分段 ID | 3cc4021b-706b-457e-8296-799b75c12072 |

### 数据来源

| 数据集 | 文件数 | 状态 |
|--------|--------|------|
| SCL 法律法规 | 28953 个 .txt | ✅ 已入库（22552 部法律） |
| LeCaRD 案例检索 | 3795 个 .txt | ⚠️ 部分入库（1019 条待处理） |
| 自定义文档 | data/docs/ | ✅ 已入库 |

### 文件路径

```
data/chroma/chroma.sqlite3                    # 主数据库（2.6GB）
data/chroma/3cc4021b-.../                     # HNSW 索引分段目录
data/datasets/scl/texts/                      # SCL 文本文件（28953 个）
data/datasets/lecard/texts/                   # LeCaRD 文本文件（3795 个）
data/chroma/bm25_cache/                       # BM25 索引缓存
```

---

## 四、修复方案（待确认）

### 方案 A：重建 ChromaDB 集合（推荐）

**步骤**：
1. 从现有 SQLite 导出所有数据（ids, documents, metadatas, embeddings）
2. 删除损坏的集合
3. 创建新集合
4. 重新导入数据（ChromaDB 会自动重建 HNSW 索引）

**预计时间**：45-80 分钟

**风险**：导出过程中如果 `col.get(include=['embeddings'])` 也失败，需要从源文件重新入库。

### 方案 B：从源文件重新入库

**步骤**：
1. 删除损坏的 ChromaDB 数据
2. 重新运行 SCL 入库：`PYTHONPATH=. uv run python scripts/ingest.py --split-mode legal data/datasets/scl/texts/`
3. 重新运行 LeCaRD 入库：`PYTHONPATH=. uv run python scripts/ingest.py --split-mode legal data/datasets/lecard/texts/`

**预计时间**：60-120 分钟（需要 GPU 生成 embedding）

**风险**：无，但耗时更长。

### 方案 C：仅修复 HNSW 索引

**步骤**：
1. 尝试让 ChromaDB 自动重建 HNSW 索引（可能需要触发 compaction）
2. 如果失败，手动删除 HNSW 分段目录，让 ChromaDB 重建

**预计时间**：5-15 分钟

**风险**：可能无法修复，需要回退到方案 A 或 B。

---

## 五、环境信息

```
操作系统：Windows 11 Pro
Python：3.14+
ChromaDB：1.5.x
GPU：RTX 5060 8GB
项目路径：C:\Users\Admin\Desktop\RAG
```

---

## 六、相关文件

| 文件 | 说明 |
|------|------|
| `app/rag/vector_store.py` | 向量存储模块（已修复 get_all_documents） |
| `app/rag/retriever.py` | 混合检索模块（调用失败） |
| `app/rag/embedding.py` | 嵌入模型模块 |
| `scripts/ingest.py` | 入库脚本 |
| `scripts/search_test.py` | 检索测试脚本 |

---

## 七、日志记录

### 2026-07-12 00:13 - SCL 入库完成

```
ChromaDB 文档片段总数: 367369
ChromaDB 文件大小: 1.2GB
```

### 2026-07-12 00:44 - LeCaRD 入库进行中

```
ChromaDB 文件大小: 2.1GB → 2.2GB → 2.4GB
进程 PID: 44664（内存占用 3GB）
```

### 2026-07-12 01:01 - LeCaRD 入库完成（但进程异常）

```
SQLite 嵌入总数: 656687
待处理队列: 1019
HNSW pickle 文件: 截断（10562001 字节）
```

### 2026-07-12 01:02 - 发现 HNSW 索引损坏

```
错误: Error deserializing pickle file: eval error at offset 29609312: trailing bytes found
修复: 删除 index_metadata.pickle
结果: col.get() 恢复正常，col.query() 仍有问题
```

### 2026-07-12 01:05 - 混合检索测试失败

```
错误: Error finding id
get_all_documents() SQL 变量溢出 → 已修复为分批获取
混合检索仍然失败
```
