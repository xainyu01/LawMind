# 项目进度与检查单

> 最后更新：2026-06-05

---

## 总览

| 阶段 | 状态 | 说明 |
|------|------|------|
| 第一阶段：环境搭建 | ✅ 完成 | uv 管理、依赖安装、.env 配置 |
| 第二阶段：文档解析与向量化 | ✅ 完成 | 加载→分段→嵌入→ChromaDB，CUDA 已通 |
| 第三阶段：RAG 检索与 LLM 生成 | ✅ 完成 | 混合检索、重排序、/chat 接口 |
| 第四阶段：对话记忆与缓存 | ✅ 完成 | 双后端、对话记忆、语义缓存 |
| 第五阶段：前端与评估 | 🔧 进行中 | Streamlit 界面 ✅、RAGAS 评测 ⏳ |

---

## 第一阶段 ✅ 环境搭建

- [x] uv 安装并管理项目
- [x] pyproject.toml 配置完成
- [x] 所有依赖安装（FastAPI / LangChain / ChromaDB / Redis / sentence-transformers 等）
- [x] `.env` 配置（DeepSeek API Key / 模型名 / HF 镜像）
- [x] `main.py` FastAPI 入口，`/health` 正常返回
- [x] 目录结构就绪

## 第二阶段 ✅ 文档解析与向量化

### 已完成的文件

| 文件 | 功能 | 状态 |
|------|------|------|
| `app/core/config.py` | 统一配置加载 | ✅ |
| `app/rag/document_loader.py` | PDF/Word/TXT 加载与分段 | ✅ |
| `app/rag/embedding.py` | BGE 模型封装，GPU 推理 | ✅ |
| `app/rag/vector_store.py` | ChromaDB 增删查 | ✅ |
| `scripts/download_model.py` | 模型下载 | ✅ |
| `scripts/ingest.py` | 一键入库 | ✅ |
| `scripts/search_test.py` | 检索验证 | ✅ |

### 端到端验证通过

- 测试文档 `data/docs/民法典节选.txt` 入库成功
- 检索"夫妻义务"→ 返回第1043条
- 检索"离婚条件"→ 返回相关法条

### 解决的关键问题

| 问题 | 方案 |
|------|------|
| numpy MINGW 崩溃 (Python 3.14) | 升级依赖链 → numpy 2.4.6 |
| huggingface 被墙 | `.env` 设 `HF_ENDPOINT=https://hf-mirror.com` |
| PyTorch CPU 版反复覆盖 | `pyproject.toml` 加 `[tool.uv.sources]` 锁定 CUDA 索引 |
| langchain 1.x `Document` 路径变更 | 改用 `langchain_core.documents` |

### 硬件确认

```
GPU:        NVIDIA GeForce RTX 5060 (8GB)
PyTorch:    2.11.0+cu128, CUDA: True
嵌入模型:   BAAI/bge-small-zh (~100MB)
LLM:        DeepSeek API（云端）
剩余显存:   ~7GB
```

---

## 第三阶段 ✅ RAG 检索与 LLM 生成

- [x] `app/rag/retriever.py` — 混合检索（BM25 + 向量 + RRF 融合）
- [x] `app/rag/reranker.py` — Cross-Encoder 重排序
- [x] `app/rag/generator.py` — LLM 调用 + Prompt 模板 + 流式生成
- [x] `app/api/routes.py` — `/chat` 问答接口
- [x] `app/core/config.py` — 扩展 Phase 3 配置项
- [x] `main.py` — 注册 `/api/v1` 路由
- [x] `scripts/search_test.py` — 升级支持混合检索
- [x] 端到端验证：启动服务 + /chat 接口测试 + 法条溯源确认

## 第四阶段 ✅ 对话记忆与缓存

- [x] `文档/phase4-对话记忆与缓存.md` — Phase 4 实施指南
- [x] `diskcache` 安装（Windows 本地存储后端）
- [x] `app/db/__init__.py` — 空 init
- [x] `app/db/backend.py` — Redis + Disk 双后端（自动探测降级）
- [x] `app/db/memory.py` — 对话记忆 + 上下文提取
- [x] `app/db/cache.py` — 语义缓存（embedding 相似度 > 0.92 命中）
- [x] `/chat` 与 `app.py` 集成记忆/缓存

## 第五阶段 🔧 前端与评估

- [x] Streamlit 聊天界面
- [ ] 文件上传功能
- [ ] RAGAS 评测
- [ ] 用户反馈收集（点赞/点踩）

---

## 下一步待办

### 优先级高

1. **RAGAS 评测**：搭建评测框架，评估答案忠实度、上下文相关性、法条引用准确率
2. **文件上传**：Streamlit 界面支持上传 PDF/Word/TXT 文档并入库

### 优先级中

3. **用户反馈**：添加点赞/点踩按钮，记录用户反馈
4. **法条时效性检查**：元数据标注颁布/废止日期，检索时过滤已废止法条

### 优先级低

5. **LLM 意图分类**：用轻量 Prompt 替换关键词方案
6. **监控接口**：Prometheus 指标暴露
